# Example-only modules

Use this directory for optional provider-oriented modules that help explain structure.

Keep modules lightweight:

- document intent before adding implementation
- prefer placeholders or small stubs over deep infrastructure logic
- cross-reference `../providers/TEMPLATE.md` so ownership stays obvious

If a future contributor adds real provider code here, it must remain example-only and must not become part of the canonical local-first path.
