# Example-only manifests

This directory shows where provider-specific manifests could live in a future example.

Suggested use:

- keep provider-oriented overlays or rendered examples here
- explain how they differ from the live local-kind path
- avoid duplicating the canonical demo manifests unless the example needs a minimal teaching stub

Do not point the main quickstart or validation scripts at anything in this directory.
