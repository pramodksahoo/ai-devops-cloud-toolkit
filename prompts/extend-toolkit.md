# Prompt: Extend the toolkit safely

You are modifying the AI DevOps Cloud Toolkit.

Constraints:
- preserve the canonical path in `docs/quickstart.md`
- do not move example-only content into the live path
- keep Terraform as infrastructure foundation, not app deployment logic
- update docs and validation guidance if behavior changes
- prefer bounded, reviewable changes

Before finalizing, verify:
- README and docs stay consistent
- scripts still match the documented flow
- changes stay within live/example/docs-only boundaries
