# Prompt: Add an example-only extension

You are adding example-only material to this repo.

Rules:
- put assets under `examples/` or `docs/examples/`
- do not wire the example into the canonical quickstart by default
- explain how the example relates to the live MVP
- keep it lightweight and easy to remove
